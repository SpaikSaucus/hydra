# Normalize text files; keep shell scripts LF (they run inside Linux containers)
# and batch files CRLF, regardless of the platform that clones the repo.
* text=auto
*.sh text eol=lf
gradlew text eol=lf
*.bat text eol=crlf
*.png binary
*.jar binary
*.keystore binary
*.apk binary
